---
name: 文字提示
route: /text_tip
menu: 反馈
---


import { Playground, Props } from 'docz';
import TextTip from './index';

import './demo.less';

# 文字提示
<Props of={TextTip} />

## 基本使用
<Playground>
    <TextTip tip='这个真的超级流弊。。。'>
            {
              (startShow, ref) => {
                return (
                    <a href="javascript:void(0);"
                       ref={ref}
                       style={{display: 'inline-block'}}
                       onClick={startShow}>点击我试试</a>
                )
              }
            }
    </TextTip>
</Playground>